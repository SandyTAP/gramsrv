The gift pack manifest, asset resolver, and bundle builder were adapted from
`owpengram/owpengram-server` `dev` at commit `55c3e84` (Apache License 2.0).
The license text is in `LICENSE-owpengram.txt` in this directory.

Changes for telesrv include strict manifest parsing, ZIP path and size checks,
full-pack prevalidation, bounded catalog capacity, disabled-gift duplicate
detection, audit command integration, and decimal-string gift IDs.
